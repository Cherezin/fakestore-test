/*interface Products {
    id: number,
    title: string,
    image: string,
    price: number,
    description: string,
}

const ProductCard: React.FC<{ products: Products }> = ({ products }) => {
    <div>

    </div>
}

export default ProductCard;*/